{
  "name": "oblivian-web",
  "version": "1.0.0",
  "private": true,
  "description": "Oblivian — app web estático (deploy Cloudflare Pages)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/build.mjs && wrangler pages deploy dist",
    "preview": "node scripts/build.mjs && npx serve dist",
    "apk": "node scripts/apk-release.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
